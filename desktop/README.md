# Desktop Applications

Workspace for shared desktop application standards and prototypes.

Each released utility should eventually live in its own repository. This directory is reserved for:

- shared application shell experiments
- common UI components
- updater protocol prototypes
- localization resources
- packaging conventions
- example applications

Product rule: **One Tool. One Task. Done.**
